import { HighligtVoteDirective } from './highligt-vote.directive';

describe('HighligtVoteDirective', () => {
  it('should create an instance', () => {
    const directive = new HighligtVoteDirective();
    expect(directive).toBeTruthy();
  });
});
